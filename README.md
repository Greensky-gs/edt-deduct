# EDT-DEDUCT

Side project for fun, written in C

## Usage

1. Clone (or download)
1. Compile (using **gcc**, with the [makefile](./makefile) : `make`)
1. Start : `./main.exe` (or both steps with `make launch -s`)
1. You have a cli, try to guess how it works
